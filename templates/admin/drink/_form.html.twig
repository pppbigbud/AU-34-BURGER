<div class="containerNewDrinkForm">
    <div class="fondFormDrinkNew">
        {{ form_start(form) }}
        <div class="row">
            <div class="col drinkFormNew">
                {{ form_label(form.Name) }}
                {{ form_widget(form.Name) }}
            </div>
        </div>

        <div class="row">
            <div class="col drinkFormNew">
                {{ form_label(form.Size) }}
                {{ form_widget(form.Size) }}
            </div>
        </div>

        <div class="row">
            <div class="col drinkFormNew">
                {{ form_label(form.Price) }}
                {{ form_widget(form.Price) }}
            </div>
        </div>

        <div class="row">
            <div class="col drinkFormNew">
                {{ form_label(form.imageFile) }}
                {{ form_widget(form.imageFile) }}
            </div>
        </div>

        <button class="btnDrinkNew btnFormValid">{{ button_label|default('Enregistrer une nouvelle boisson') }}</button>
        <button class="btnDrinkNew btnFormAnnul"><a href="{{ path('app_drink_index') }}">Annuler / Retour à la
                liste</a></button>
        {{ form_end(form) }}
    </div>
</div>
